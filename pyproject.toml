[project]
name = "distance-routing"
version = "0.1.0"
description = "Optimizaciones de rutas y bodegas con distancia Manhattan en 3D"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "networkx>=3.6.1",
]
